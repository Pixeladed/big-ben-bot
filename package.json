{
  "name": "big-ben-bot",
  "version": "1.0.0",
  "author": "",
  "description": "Big Ben Bot for Discord",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/main.ts --bundle --outfile=build/index.js"
  },
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.12.9",
    "typescript": "^4.3.4"
  }
}