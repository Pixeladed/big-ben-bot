{
  "name": "big-ben-bot",
  "version": "1.0.0",
  "author": "",
  "description": "Big Ben Bot for Discord",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/main.ts --bundle --outfile=build/main.js --platform=node",
    "dev": "npm run build -- --watch",
    "start": "node build/main"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^15.12.5",
    "esbuild": "^0.12.9",
    "typescript": "^4.3.4"
  },
  "dependencies": {
    "@discordjs/opus": "^0.5.3",
    "discord.js": "^12.5.3",
    "ffmpeg-static": "^4.4.0"
  }
}